version: '2'
services:
  db:
    image: harrifeng/mariadb:10.1-frappe
    environment:
      - MYSQL_ALLOW_EMPTY_PASSWORD=yes      
    volumes:
      - ./custom:/etc/mysql/conf.d
  web:
    build: .
    depends_on:
      - db
    
